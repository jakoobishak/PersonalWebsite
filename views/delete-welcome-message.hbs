{{#if hasDatabaseError}}
<p>Database failed to fetch the data. Please try again later.</p>
{{else}}
{{#if errors.length}}
<p>Couldn't delete the welcome message because:</p>
<ul>
    {{#each errors}}
    <li class="errors">{{this}}</li>
    {{/each}}
</ul>
{{/if}}
<h1>Delete welcome message</h1>
<p>Are you sure you want to delete the welcome message named {{welcome.title}}?</p>
<form method="POST" action="/{{welcome.id}}/delete">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <input type="submit" value="Delete" class="btn btn-primary">
</form>
{{/if}}